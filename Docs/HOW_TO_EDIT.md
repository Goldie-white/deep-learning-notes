# 如何编辑博客文章（无需写代码）

## 📝 方法一：直接编辑 Markdown 文件（最简单）

### 修改现有文章

1. **找到文章对应的 Markdown 文件**
   - ResNet 文章：编辑 `ResNet.md` 文件
   - 以后添加的文章：编辑对应的 `.md` 文件

2. **用任何文本编辑器打开文件**（推荐使用 VS Code、Notepad++ 或记事本）

3. **直接修改内容**
   - 修改标题、段落、公式等
   - 支持 Markdown 语法（标题用 `#`，加粗用 `**文字**`，公式用 `$...$` 或 `$$...$$`）

4. **保存文件并推送到 GitHub**
   ```bash
   git add ResNet.md
   git commit -m "Update ResNet article"
   git push origin main
   ```

### 添加新文章

1. **创建新的 Markdown 文件**
   - 例如：`Transformer.md`、`Attention.md` 等
   - 文件名建议使用英文，避免空格

2. **编写文章内容**
   - 使用 Markdown 格式
   - 第一行通常是标题：`# 文章标题`

3. **在 `articles.json` 中添加文章信息**
   ```json
   {
       "id": 2,
       "title": "Transformer",
       "excerpt": "文章摘要...",
       "category": "architecture",
       "date": "2024-01-25",
       "readTime": "20分钟",
       "contentFile": "Transformer.md"
   }
   ```

4. **保存并推送**
   ```bash
   git add Transformer.md articles.json
   git commit -m "Add new article: Transformer"
   git push origin main
   ```

## 📋 文章分类

- `architecture` - Model Architecture（模型架构）
- `paper` - Paper Reading（论文解读）

## 📅 日期格式

使用格式：`YYYY-MM-DD`，例如：`2024-01-20`

## ⏱️ 阅读时间

格式：`数字分钟`，例如：`15分钟`、`20分钟`

## 💡 提示

- **Markdown 语法**：
  - 标题：`# 一级标题`、`## 二级标题`、`### 三级标题`
  - 加粗：`**文字**`
  - 斜体：`*文字*`
  - 列表：`- 项目` 或 `* 项目`
  - 链接：`[文字](网址)`
  - 引用：`> 引用文字`
  - 行内公式：`$公式$`
  - 块级公式：`$$公式$$`

- **公式示例**：
  ```
  行内公式：$H(x) = F(x) + x$
  
  块级公式：
  $$
  x_{l+1} = x_l + F(x_l, W_l)
  $$
  ```

## 🚀 快速开始

1. 打开 `ResNet.md` 文件
2. 修改你想改的内容
3. 保存文件
4. 运行：
   ```bash
   git add ResNet.md
   git commit -m "Update article"
   git push origin main
   ```
5. 等待 1-2 分钟，刷新网站即可看到更新！

